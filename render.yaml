databases:
  - name: rails_shopping_list
    databaseName: rails_shopping_list
    user: rails_shopping_list

services:
  - type: web
    name: rails_shopping_list
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: rails_shopping_list
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false